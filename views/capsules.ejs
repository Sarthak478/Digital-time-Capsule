<!DOCTYPE html>
<html>
<head>
  <title>Time Capsule</title>
  <style>
    body { font-family: sans-serif; margin: 2rem; }
  </style>
</head>
<body>
  <h1>Capsule</h1>
  <% if (isUnlocked) { %>
    <p><strong>Message:</strong> <%= capsule.message %></p>
  <% } else { %>
    <p>This capsule is locked until <%= capsule.unlockDate %>.</p>
  <% } %>
  <a href="/">Back</a>
</body>
</html>